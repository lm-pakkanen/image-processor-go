{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch image processor",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/run.go"
        }
    ]
}